#include <stdio.h>
#include <stdlib.h>
#include <time.h>


int main()
{
	int* table = int[20];
	int i, r;
	for(i = 0; i < 20; i += 1)
	{
		srand(time(NULL));
		r = rand();
		table[i] = r;
	}

	for(i = 0; i < 20; i += 1)
	{
		printf("Value: %d", table[i]);
	}
}
