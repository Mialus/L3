#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
	int tab [20];
	int i;
	srand(time(NULL));
	for(i = 0; i<20; i++)
	{
		tab[i]= rand();
		printf("%d\n", tab[i]);
	}

	return 0;
}
